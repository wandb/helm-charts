name: Pre-Release Charts

on:
  pull_request:
    branches:
      - main

jobs:
  release:
    runs-on: ubuntu-latest

    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@v2
        with:
          fetch-depth: 0

      - name: Configure Git
        run: |
          git config user.name "$GITHUB_ACTOR"
          git config user.email "$GITHUB_ACTOR@users.noreply.github.com"

      - name: Install Helm
        uses: azure/setup-helm@v4.2.0
        with:
          version: v3.20.1

      - name: Set up chart-testing
        uses: helm/chart-testing-action@v2.8.0
        with:
          version: v3.11.0

      - name: yq - portable yaml processor
        uses: mikefarah/yq@v4.44.3
        with:
          cmd: ""

      - name: Install Helm Deps
        run: |
          helm repo add prometheus-community https://prometheus-community.github.io/helm-charts
          helm repo add stakater https://stakater.github.io/stakater-charts
          helm repo update

      - name: Build deps install script
        run: |
          chmod +x ./helm-dep-build.sh
          ./helm-dep-build.sh
        shell: bash

      - name: Package charts
        id: package
        env:
          PR_NUMBER: ${{ github.event.number }}
        run: |
          set -euo pipefail
          git_hash=$(git rev-parse --short "$GITHUB_SHA")
          changed=$(ct list-changed --config ct.yaml)
          if [ -z "$changed" ]; then
            echo "No changed charts detected; skipping package + release."
            echo "packaged=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          for chart in $changed; do
            helm package --version=$(helm show chart "${chart}" | yq .version)-PR${PR_NUMBER}-${git_hash} -d .cr-release-packages "${chart}"
          done
          echo "packaged=true" >> "$GITHUB_OUTPUT"
        shell: bash

      - name: Run chart-releaser
        if: steps.package.outputs.packaged == 'true'
        # Pinned to v1.5.0 because v1.6.0+ regressed with skip_packaging:
        # https://github.com/helm/chart-releaser-action/issues/171 — cr.sh
        # references $latest_tag outside the branch that initialises it,
        # so any run with skip_packaging fails with "latest_tag: unbound
        # variable" at the end.
        uses: helm/chart-releaser-action@be16258da8010256c6e82849661221415f031968 # v1.5.0
        with:
          skip_packaging: true
        env:
          CR_TOKEN: "${{ secrets.GITHUB_TOKEN }}"
