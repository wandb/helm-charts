{{- if .Values.service.enabled }}
{{- $loadBalancer := mergeOverwrite .Values.global.loadBalancer .Values.service.loadBalancer }}
{{- if and (eq .Values.global.cloudProvider "gcp") $loadBalancer.enabled }}
---
apiVersion: cloud.google.com/v1
kind: BackendConfig
metadata:
  name: {{ include "wandb-base.fullname" . }}-backend-config
  labels:
    {{- include "wandb-base.labels" . | nindent 4 }}
spec:
  {{- if .Values.global.gcpSecurityPolicy }}
  securityPolicy:
    name: {{ .Values.global.gcpSecurityPolicy }}
  {{- end }}
  timeoutSec: {{ $loadBalancer.timeoutSec }}
  connectionDraining:
    drainingTimeoutSec: {{ $loadBalancer.connectionDraining.drainingTimeoutSeconds }}
  healthCheck:
    path: {{ $loadBalancer.healthCheck.path }}
    checkIntervalSec: {{ $loadBalancer.healthCheck.checkIntervalSeconds }}
    timeoutSec: {{ $loadBalancer.healthCheck.timeoutSeconds }}
    healthyThreshold: {{ $loadBalancer.healthCheck.healthyThreshold }}
    unhealthyThreshold: {{ $loadBalancer.healthCheck.unhealthyThreshold }}

{{- end }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "wandb-base.fullname" . }}
  labels:
    {{- include "wandb-base.labels" . | nindent 4 }}
    {{- if .Values.service.labels -}}
    {{-   toYaml .Values.service.labels | nindent 4 }}
    {{- end }}
  annotations:
    {{- tpl (include "wandb-base.commonAnnotations" . | nindent 4) . }}
    {{- tpl (include "wandb-base.serviceAnnotations" . | nindent 4) . }}
    {{- if $loadBalancer.enabled }}
      {{- if eq .Values.global.cloudProvider "aws" }}
    service.beta.kubernetes.io/aws-load-balancer-healthcheck-healthy-threshold: "{{ $loadBalancer.healthCheck.healthyThreshold }}"
    service.beta.kubernetes.io/aws-load-balancer-healthcheck-unhealthy-threshold: "{{ $loadBalancer.healthCheck.unhealthyThreshold }}"
    service.beta.kubernetes.io/aws-load-balancer-healthcheck-interval: "{{ $loadBalancer.healthCheck.checkIntervalSeconds }}"
    service.beta.kubernetes.io/aws-load-balancer-healthcheck-timeout: "{{ $loadBalancer.healthCheck.timeoutSeconds }}"
        {{- if $loadBalancer.healthCheck.path }}
    alb.ingress.kubernetes.io/healthcheck-path: {{ $loadBalancer.healthCheck.path }}
        {{- end }}
      {{- end }}
      {{- if eq .Values.global.cloudProvider "gcp" }}
    cloud.google.com/neg: '{"ingress": true}'
    cloud.google.com/backend-config: '{"default": "{{ include "wandb-base.fullname" . }}-backend-config"}'
      {{- end }}
    {{- end }}
spec:
  type: {{ .Values.service.type }}
  ports:
    {{- with .Values.service.ports }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
  selector:
    {{- include "wandb-base.selectorLabels" . | nindent 4 }}
{{- end }}
