{{- $secretName := (include "wandb.kafka.client.passwordSecret" .)  }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ $secretName }}
  labels:
    {{- include "wandb.commonLabels" . | nindent 4 }}
data:
  KAFKA_CLIENT_PASSWORD: {{ include "wandb.kafka.client.password" . | b64enc }}