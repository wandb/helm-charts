{{- /*
This file is intentionally left empty as SMTP configuration is now handled
entirely through environment variables in _env.tpl following the same pattern
as MySQL configuration where each field can be either a string or a map with
valueFrom for Kubernetes secret references.
*/ -}}
