{{- if .Values.airgapped }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "name" . }}-chart-configmap
data:
  {{- range .Values.charts }}
    {{ .name }}.b64: {{ $.Files.Get .path | b64enc }}
  {{- end }}
{{- end }}
