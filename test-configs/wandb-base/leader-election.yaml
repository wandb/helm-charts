leaderElection:
  enabled: true
  leaseName: gorilla-migrate-leader

containers:
  app:
    image:
      repository: debian
      tag: 12-slim
    command:
      - /bin/bash
    args:
      - -c
      - sleep infinity
