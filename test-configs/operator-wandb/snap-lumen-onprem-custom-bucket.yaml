global:
  cloudProvider: ""
  lumen:
    dataRoot: s3://customer-onprem-bucket/lumen
    publish:
      gorilla:
        enabled: true

lumen-agent:
  install: true

lumen-notebook:
  install: true

lumen-processor:
  install: true