global:
  extraEnv:
    TAG_CUSTOMER_NS: wandb-s-perf-int
  datadog:
    rum:
      enabled: true
      profilingSampleRate: 10
      sessionSampleRate: 100
      sessionReplaySampleRate: 25
      traceSampleRate: 100

app:
  install: false

frontend:
  install: true
